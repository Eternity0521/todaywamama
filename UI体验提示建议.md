# UI 体验提示建议

> 基于 2026-09-03 真机调试与用户反馈。原则：每个环节都让用户知道「下一步做什么 / 为什么没反应」。

## 高优先（建议近期做）

1. **分享卡页常驻提示条**：卡片下方一行小字「长按图片可直接保存或发送」。真机结论：国产浏览器（小米/夸克）下载只能进浏览器目录，存相册只能靠长按 —— 不提示用户就找不到。
2. **「我的设定」保存反馈**：保存后提示「明日生效，今日运势不变」。目前改完偏好第二天才变化，不提示会被当成 bug。
3. **Toast 时长按文案长度**：分享类 toast 文案 30+ 字，2 秒读不完。建议文案 ≥12 字时延长到 3.5–4 秒（`shareToast` 动画现为 2s）。
4. **抽卡页首次手势提示**：「左右滑动选牌，点中间抽取」，抽过一次后不再显示（localStorage 标记）。桌面用户不知道可拖拽。
5. **弱兼容浏览器提示条**：检测到夸克/UC（UA 已有识别逻辑）时，首页顶部提示「本浏览器部分功能受限，建议用 Chrome 或微信打开」。真机结论：夸克分享完全不可用。

## 中优先

6. onboarding 跳过用户：首页欢迎态提一句「可随时在『我的设定』补全偏好」。
7. 「改命」入口接回（core 已就绪）：结果页入口 + 确认弹窗 + 每天一次（PRD §26 文案「命运并非不可改变」）。

## 已具备（勿重复做）

1★ 娱乐局兜底文案、页脚免责声明、`prefers-reduced-motion` 降级、aria-label、生成中按钮态、长按兜底 toast —— 均已实现。
