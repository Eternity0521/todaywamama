# 开局运势（GG Oracle）功能完成记录

> 版本：H5 V0.2（UI 融合版）· 记录日期：2026-08-31
> 依据：《游戏前测运势小程序 PRD V0.1》与《开发指导书.md》
> 范围：PRD §28 定义的 **P0 完整版** + **美术设计稿 UI 融合**，全部完成

## 一、产品定位

不是游戏数据工具，而是玩家开局前的仪式工具：**抽卡 → 揭晓 → 带着结果进入游戏**，全程 ≤30 秒。唯一游戏：VALORANT。

## 二、总体状态

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 脚手架与内容库 | Vite 项目 + core 层 + 6 个内容库 | ✅ 完成 |
| M2 核心算法 | 确定性生成器 + localStorage 持久化 + 改命 | ✅ 完成 |
| M3 页面流程 | 5 页流程 + 3D 动画 + 埋点 | ✅ 完成 |
| M4 分享卡 | Canvas 绘制 + 保存/复制 | ✅ 完成 |
| M5 打磨与部署准备 | 文案规范检查 + 构建验证 + 部署说明 | ✅ 完成 |

**质量状态**：34/34 单元测试通过；TypeScript 严格模式零错误；生产构建 74KB（gzip）。

## 三、已完成功能清单

### 3.1 页面流程（5 个页面，线性状态机 home → pick → flip → result → share）

| 页面 | 功能 | 对应 PRD |
|---|---|---|
| 首页 | 品牌「今日瓦运」+ 日期 + 大抽卡按钮；**首次访问**显示欢迎文案「今天这把，有说法。」+ 4 条 benefit；**有历史时**显示昨日运势（星级 + 昨日关键词）；已有今日结果时按钮变「查看今日运势」 | §08、§11 |
| 抽卡 | 三张 CSS 绘制的卡背，「凭感觉选一张」，点击后 3D 翻转、其余淡出 | §12 |
| 翻牌揭晓 | 3D 翻转入场 → 星级逐颗渐显（每颗 120ms）→ 主运标题/描述淡入；支持 `prefers-reduced-motion` 降级 | §12、§13 |
| 完整运势 | 7 个分节：今日总运 / 幸运位置（含四位置指数）/ 幸运英雄 / 幸运武器（含今日不推荐）/ 幸运皮肤（含幸运颜色与饰品）/ 地图运势排行 / 今日 Advice；底部「保存今日瓦运」「发给队友」+「改命」入口 | §13–§20 |
| 分享卡 | Canvas 生成 750×1334（@2x 渲染）图片，含品牌/日期/星级/关键词/六大字段/雷区/今日建议；支持下载按钮、长按保存兜底、复制文案 | §21 |

### 3.2 核心机制

| 机制 | 实现 | 对应 PRD |
|---|---|---|
| **每日 Seed** | `hash(userId | 日期)` 确定性种子（xmur3 + mulberry32），同人同日无论重进、刷新、选哪张卡，结果逐字段一致 | §26 |
| **半随机算法** | 位置 = 随机档 70 + 近期未推荐档 20 + 特殊事件档 10（P0 事件档空置）；近 7 天未出现的位置权重 +20；连续 400 天模拟验证同一位置最长连推 ≤7 天。英雄近 7 天推荐过权重降为 1/3。偏好档 30 已预留 P1 挂点 | §25 |
| **改命** | 每天一次：种子追加 `|r1` 整体重抽并覆盖保存；入口文案「命运并非不可改变」，二次点击确认，用后消失 | §26 |
| **星级分布** | 主运星级 1★4% / 2★16% / 3★30% / 4★32% / 5★18%；每星级 3 个标题 + 3 句描述，1★ 强制「娱乐局」轻松框架 | §24、§31.4 |
| **每日刷新** | 日期按本地时区计算，跨 0 点自动换种子，昨日结果自动进入历史 | §26 |

### 3.3 数据持久化（纯前端，无后端）

- 单一 localStorage key（`gg.state.v1`）：用户 ID + 全部运势历史
- 历史保留 90 天，按日期降序，超出丢弃最旧
- 用户 ID 首次生成（crypto.randomUUID，含降级），隐私：数据仅存本地不上传

### 3.4 内容库（文案全部游戏梗风格，无星座腔）

| 库 | 规模 | 说明 |
|---|---|---|
| 英雄 | 27 个（决斗 8 / 先锋 7 / 控场 6 / 哨卫 6） | 每个 3–5 关键词 + 2 条趣味解释（含 PRD 原句「今天别人看不到你的时候，往往就是你的机会」） |
| 武器 | 8 个 | 每个 2 条推荐理由 + 2 条「不推荐」梗文案（含 PRD 原句「今天的你可能会忍不住 Peek 第二次」） |
| 皮肤 | 20 个 + 10 个饰品 | 每个含幸运颜色候选 + 2 条契合文案；模式 A 全池随机 |
| 地图 | 5 个 | 星级标签池（上分圣地/可以一战/五五开/容易坐牢/今天最好别碰） |
| Advice | 16 条 | 关键词 + 解释（别急 / 少干拉 / DON'T REPEEK / 输两把就休息…） |
| 主运 | 15 标题 + 15 描述 | 覆盖 1–5 星，含 PRD 原句 |

### 3.5 埋点（北极星指标数据源，PRD §33）

8 个事件全部接入（`src/analytics.ts` 统一出口，当前 console 输出，接入统计服务只改此文件）：

`view_home` / `test_start` / `card_pick` / `fortune_complete` / `share_open` / `share_save` / `share_copy` / `reroll_click`

北极星计算：完成率 = `fortune_complete` / `test_start`（目标 ≥80%）；分享率 = (`share_save` + `share_copy`) / `fortune_complete`（目标 ≥15%）。

## 四、技术实现摘要

| 项 | 实现 |
|---|---|
| 技术栈 | Vite 8 + React 19 + TypeScript（严格模式）+ 原生 CSS（设计 token）+ Canvas 2D + Vitest 4 |
| 架构 | `core/` 平台无关（类型/算法/内容库/存储接口，纯函数无 DOM 依赖）↔ `src/` H5 壳（UI + localStorage + Canvas）——为「小程序随后」迁移预留 |
| 设计 | 深色电竞风：深蓝黑底 + 霓虹紫/青 accent + 金色星级，全 token 化；emoji 图标零图片资源 |
| 体积 | 生产构建 gzip 74KB（JS 73.9 + CSS 2.5 + HTML 0.6） |
| 无障碍 | `prefers-reduced-motion` 全局动画降级；按钮 aria-label；星级 aria-label |
| 兼容 | 微信内置浏览器（长按保存兜底、execCommand 复制降级）、safe-area 适配、`base: './'` 可部署任意路径 |

## 五、测试覆盖（34 条，Vitest）

| 模块 | 覆盖点 |
|---|---|
| 确定性随机（13 条） | 哈希稳定性、PRNG 序列一致性、值域/均匀性、加权抽样分布（±2%）、洗牌确定性 |
| 生成器与编排（15 条） | 同参 deep-equal、种子隔离（人/日/改命）、位置权重规格、星级分布、地图排列、契合度区间、400 天连推去重、Advice 跨日去重、getOrCreateToday 幂等、改命仅一次、昨日运势跨月 |
| 存储（4 条） | 读写往返、同日覆盖、跨月排序、90 天上限 |
| 分享卡渲染（2 条） | 真实 Canvas 渲染不抛错、输出 1500×2668 合法 PNG（文件头校验）、不同运势图片不同 |

## 六、待真机验证清单（自动化无法覆盖）

- [ ] 手机微信内打开：分享卡**长按保存**成功（iOS + Android）
- [ ] 「保存图片」按钮行为（iOS Safari 的 a[download] 需长按兜底）
- [ ] 改命后刷新页面，结果仍为改命后的运势
- [ ] 375px 窄屏下各页不破版
- [ ] 跨 0 点后运势自然刷新、昨日运势正确显示

## 七、未实现内容（后续版本，PRD 已定义）

| 版本 | 内容 |
|---|---|
| P1 | 玩家偏好设置（权重挂点已留）、稀有卡 70/20/8/2、皮肤库模式 B（只测我拥有的）、好友运势、双排组合、五排阵容、每日签到、历史运势、图鉴、底部四 Tab、今日游戏方式 |
| P2 | 多游戏接入（LOL / 王者荣耀 / CS2 / Apex / OW），core 已按内容库与维度参数化设计 |
| 迁移 | 微信小程序：`core/` 原样复用，仅替换 UI 壳与存储/Canvas 实现（迁移清单见指导书 §13） |

## 八、UI 融合（美术设计稿，V0.2）

> 设计稿来源：同学产出《移动应用设计方案》（`design/unpacked/`，方向「电竞终端数据感 · 霓虹紫」，3 个方向探索 + 5 屏交互 Demo）。

| 项 | 内容 |
|---|---|
| 视觉基调 | 紫黑 `#0c0910` + 霓虹紫 `#c084d8` + 全直角 + Noto Sans SC/JetBrains Mono 双字体 + 26px 网格背景 + 9s 扫描线 |
| 卡面机制 | 10 张自绘卡面（05/08/09 文案为设计稿原稿，其余 7 张按规格补齐）成为主运核心内容；`cardFor()` 独立确定性种子流；卡面与星级独立 |
| 宜/忌 | 新增卡面绑定字段（good/bad），揭晓页双栏 + 分享文案 + 分享卡「今日忌」格 |
| 五页重绘 | home（扇形卡背+标语）/ pick（手动翻转+真实卡面）/ reveal（卡面+星+宜忌）/ result（幽影角色卡+位置指数格+今日忌大块）/ share（保存+复制+返回首页） |
| Canvas 分享卡 | 设计稿 2×2 网格版式（角色/武器/地图/忌），750×1334 @2x 不变，星形路径手绘 |
| 素材管线 | `scripts/compress-assets.mjs`（sharp 一次性）→ 12 张 WebP 共 0.19MB；`cardAssets.ts` 映射 id→URL |
| 旧数据迁移 | `backfillMainCard()` 补齐旧结构缺的卡面字段（幂等，90 天历史保留） |
| 测试 | 43 条（新增卡面内容库 4 + 卡面生成 3 + 素材映射 2） |

**已知取舍**：1★ 日与自信卡面标题的组合由 `STAR_TONE`（娱乐局）副标兜底；角色卡固定幽影立绘（27 英雄图待补）；分享卡无用户名（P1 预留 `nickname` 选项）。

**行为变更（V0.2）**：已有今日结果时首页不再提供「直达结果页」快捷键——**每次进入都走完整仪式**（抽卡 → 卡面解读 → 运势），仪式重复展示同一份确定性结果（PRD §26 一致性不变）。

## 九、部署状态

代码仓库：https://github.com/Eternity0521/todaywamama （public，main 分支）。

线上部署未做。部署步骤见《开发指导书.md》§14（Vercel / Netlify / GitHub Pages / Cloudflare Pages 任一静态托管，必须 HTTPS）。本地体验：`npm run dev` → http://localhost:5173。
