# 开局运势（GG Oracle）H5 版开发指导书 V0.1

> 基于《游戏前测运势小程序 PRD V0.1》拆解生成。
> 本文档是 AI 开发执行的标准依据：需求有歧义时以 PRD 原文为准；PRD 未覆盖的实现细节以本文档为准。

---

## 1. 决策记录（已定，勿再讨论）

| 项 | 决策 |
|---|---|
| 平台 | **H5 先行**，验证后迁移微信小程序（架构上预留，见 §13） |
| 首版范围 | **P0 完整版**（PRD §28）；P1/P2 只预留扩展点，不实现 |
| 后端 | **无**。纯静态前端 + localStorage |
| 技术栈 | Vite + React + TypeScript + 原生 CSS（设计 token）+ Canvas 2D |
| 唯一游戏 | VALORANT（PRD §09） |
| 皮肤模式 | 模式 A：全皮肤池随机（PRD §17） |

## 2. 产品定位（一句话，所有开发判断与之对齐）

> **不是游戏数据工具，而是玩家开局前的仪式工具。**

抽卡 → 揭晓 → 带着结果进入游戏。完整测试 **≤30 秒**。
核心体验五原则（PRD §31）：**快、有仪式感、要懂游戏梗、结果不能太负面、必须可截图。**

## 3. 需求拆解（P0 功能清单与验收标准）

| # | 用户故事 | PRD 来源 | 模块 | 验收标准 |
|---|---|---|---|---|
| F1 | 打开即见「今日瓦运」+ 大抽卡按钮 + 日期 | §11 | Home | 首屏一屏内；日期格式 `YYYY / MM / DD` |
| F2 | 首次访问见欢迎文案「今天这把，有说法」 | §08 | Home 首次态 | 仅首次出现；含 4 条 benefit + CTA |
| F3 | 首页显示昨日运势（星级 + 昨日关键词） | §11 | Home | 有历史才显示；无历史隐藏不占位 |
| F4 | 三张卡背，凭感觉选一张 | §12 | CardPick | 三张牌可见；点任意一张触发翻牌 |
| F5 | 翻牌动画揭晓今日主运（星级 + 主题 + 一句话） | §12/13 | FlipReveal | 3D 翻转动画；1~5 星各有对应文案 |
| F6 | 今日总运 1–5 星 | §13 | FortuneResult | 与主运星级一致 |
| F7 | 幸运位置 + 决斗/先锋/控场/哨卫四指数 | §14 | FortuneResult | 四指数各 1–5 星；主推荐位置为 5★ 且带理由文案 |
| F8 | 幸运英雄（1 个 + 契合度% + 关键词 + 趣味解释） | §15 | FortuneResult | 契合度 88–96%；文案游戏梗风格 |
| F9 | 幸运武器（1 个 + 理由 + 今日不推荐） | §16 | FortuneResult | 不推荐项带梗文案（如「今天别信你的 Operator」） |
| F10 | 幸运皮肤（+ 幸运颜色 + 幸运饰品 + 文案） | §17 | FortuneResult | 全皮肤池随机；契合度 90–98% |
| F11 | 幸运地图排行（5 图，星级 + 评语） | §18 | FortuneResult | 首图 = 幸运地图，末图 = 今日雷区 |
| F12 | 今日 Advice（一句战术关键词 + 解释） | §20 | FortuneResult | 从梗库抽取；避免与昨日重复 |
| F13 | 分享卡：生成可保存图片 | §21/§31 | ShareCard | Canvas 生成，含全部核心字段；真机可保存 |
| F14 | 同日同人结果一致（每日 Seed） | §26 | 算法 | 刷新/重进/换卡选择，结果完全不变 |
| F15 | 「改命」每天一次重抽 | §26 | 算法+UI | 每天最多 1 次；重抽后结果改变且持久；用后按钮消失 |
| F16 | 半随机：随机 + 偏好 + 历史 + 每日种子 | §25 | 算法 | 位置权重 40/30/20/10（无偏好时按 §5.3 降级表）；连续多日不重复同一位置 |

**非功能验收（PRD §31 汇总）：**

- 全流程 ≤30 秒：从首页到结果页点击 ≤4 次
- 移动端优先，375px 宽基准；微信内置浏览器内可用，底部操作不被遮挡
- 文案**禁止星座腔**（「今日你的事业运较好」❌），必须游戏梗（「今天别 Repeek」✅）
- 1★ 也必须轻松：用「娱乐局」框架（§31.4），**禁止**「今天不适合游戏」
- 结果页本身是社交传播素材，截图友好

## 4. 技术方案

### 4.1 选型与理由

| 项 | 选择 | 理由 |
|---|---|---|
| 构建 | Vite + React 18 + TypeScript | 脚手架快、生态大、AI 开发效率最高 |
| 样式 | 原生 CSS + CSS 变量 token | 零依赖；与小程序 WXSS 心智模型一致，迁移成本低 |
| 动画 | CSS transition / transform（3D flip） | 无需动画库，性能好 |
| 分享图 | Canvas 2D 原生绘制 | 无需截图库 |
| 路由 | 不用 router，App 内**流程状态机** | P0 只有一条线性流程；迁移小程序时每步 = 一个页面 |
| 测试 | Vitest（仅 core 纯函数单测） | 只测算法与生成器，不测 UI |
| 部署 | 静态托管（Vercel / Netlify / GitHub Pages） | 无后端，需 HTTPS（微信内打开要求） |

### 4.2 分层架构（为「小程序随后」预留）

```
core/  平台无关：类型、内容数据、seeded PRNG、运势生成纯函数、存储接口定义
       迁移小程序时 100% 复用，禁止出现 DOM / localStorage / window 依赖
src/   H5 壳：React 组件（纯 UI）+ localStorage 存储实现 + Canvas 分享绘制
       迁移小程序时整体替换为 pages/，逻辑直接调 core
```

### 4.3 目录结构

```
todaywamama/
├── 游戏前测运势小程序 PRD V0.1.md   # 需求原文
├── 开发指导书.md                     # 本文档
├── CLAUDE.md                         # 会话上下文
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── core/
│   ├── types.ts                      # 数据模型（§4.4）
│   ├── random.ts                     # xmur3 + mulberry32（确定性 PRNG）
│   ├── generator.ts                  # genDailyFortune() 纯函数
│   ├── store.ts                      # FortuneStore 接口定义（不含实现）
│   └── content/
│       ├── heroes.ts                 # 英雄库（按位置分组）
│       ├── weapons.ts                # 武器库 + 理由模板
│       ├── skins.ts                  # 皮肤库 + 颜色 + 饰品
│       ├── maps.ts                   # 地图池 + 星级标签
│       ├── advice.ts                 # 战术关键词库
│       ├── cards.ts                  # 卡面库（10 张卡：title/read/宜/忌，主运核心内容）
│       └── starTone.ts               # 星级基调标签（1★ 娱乐局铁律兜底）
├── src/
│   ├── main.tsx
│   ├── App.tsx                       # 流程状态机 home→pick→flip→result→share
│   ├── storage.ts                    # localStorage 实现 FortuneStore
│   ├── analytics.ts                  # track() 埋点封装
│   ├── styles/tokens.css             # 设计 token
│   ├── ui/
│   │   ├── Home.tsx
│   │   ├── CardPick.tsx
│   │   ├── FlipReveal.tsx
│   │   ├── FortuneResult.tsx
│   │   ├── ShareCard.tsx
│   │   └── components/               # Stars、SectionCard 等小组件
│   └── share/
│       └── drawShareCard.ts          # Canvas 绘制分享卡
└── core/__tests__/                   # vitest 单测
    ├── random.test.ts
    └── generator.test.ts
```

### 4.4 数据模型（core/types.ts）

```ts
export type Role = 'duelist' | 'initiator' | 'controller' | 'sentinel'; // 决斗/先锋/控场/哨卫
export type Star = 1 | 2 | 3 | 4 | 5;

export interface MainLuck {
  stars: Star;
  title: string;      // 例：「火力全开」「娱乐局」
  desc: string;       // 例：「今日适合主动寻找机会，但不要第一时间把自己送掉。」
}

export interface PositionFortune {
  primary: Role;
  scores: Record<Role, Star>;  // 决斗指数/先锋指数/控场指数/哨卫指数
  reason: string;              // 例：「今日更适合控制节奏，而不是创造混乱。」
  heroes: string[];            // 该位置推荐 3 个英雄名（展示用，如 Omen/Clove/Viper）
}

export interface HeroFortune {
  id: string;
  match: number;       // 契合度 88–96
  keywords: string[];  // 3 个，如 冷静/信息差/偷 timing
  blurb: string;       // 一句趣味解释
}

export interface WeaponFortune {
  id: string;
  reason: string;
  avoid: { id: string; reason: string };  // 今日不推荐
}

export interface SkinFortune {
  id: string;
  match: number;       // 90–98
  color: string;       // 幸运颜色，如「绿色」
  buddy: string;       // 幸运饰品，如「RGX Butterfly」
  blurb: string;       // 例：「今天科技感会给你一点额外的自信。」
}

export interface MapFortune {
  id: string;
  stars: Star;
  label: string;       // 5★上分圣地 / 4★可以一战 / 3★五五开 / 2★容易坐牢 / 1★今天最好别碰
}

export interface Advice {
  keyword: string;     // 例：「DON'T REPEEK」
  note: string;        // 例：「今天最大的敌人不是对面，而是你觉得自己还能再打一个。」
}

export interface DailyFortune {
  date: string;        // 'YYYY-MM-DD'（本地时区）
  userId: string;
  reroll: 0 | 1;       // 0 = 今日首次，1 = 改命后
  main: MainLuck;
  position: PositionFortune;
  hero: HeroFortune;
  weapon: WeaponFortune;
  skin: SkinFortune;
  maps: MapFortune[];  // 按星级降序；[0] = 幸运地图，[last] = 今日雷区
  advice: Advice;
}

export interface FortuneStore {
  get(userId: string, date: string): DailyFortune | null;
  save(f: DailyFortune): void;
  history(userId: string, days?: number): DailyFortune[]; // 按日期降序；供昨日运势与去重算法
  getUserId(): string;   // 不存在则生成 crypto.randomUUID() 并持久化
}
```

**持久化实现（src/storage.ts）**：单一 JSON key `gg.state.v1`，结构 `{ userId: string, fortunes: DailyFortune[] }`，写时全量替换；fortunes 最多保留 90 天（超出丢弃最旧）。数据量小，无性能问题。同一天 reroll 覆盖写入（按 date 去重）。

## 5. 核心算法规格（core/generator.ts）

> 铁律：**确定性生成**。同 (userId, date, reroll) 必须逐字段产出相同结果。全项目禁止 `Math.random`。

### 5.1 确定性随机（core/random.ts）

- `xmur3(str: string): number` — 字符串 → uint32 种子
- `mulberry32(seed: number): () => number` — 种子 → [0,1) 均匀分布 PRNG
- 每日种子串：`${userId}|${date}`；改命时追加 `|r1` 即 `${userId}|${date}|r1`
- `genDailyFortune()` 内所有随机抽取**按固定代码顺序**从同一个 prng 取数——顺序写死后不允许改动（否则历史结果漂移）

### 5.2 主运星级（先抽，全局唯一）

星级分布（可调表，抽 1 次）：

| 星级 | 1★ | 2★ | 3★ | 4★ | 5★ |
|---|---|---|---|---|---|
| 概率 | 4% | 16% | 30% | 32% | 18% |

标题/描述来自卡面（`content/cards.ts` 的 `CARD_POOL`，10 张卡每张绑定 title/read/宜/忌），由**独立确定性种子流** `cardFor(userId, date, reroll)` 抽取——不消耗主 RNG，既有抽取顺序不受扰动。卡面与星级完全独立：任何星级可抽到任何卡。**1★ 文案必须是「娱乐局」框架**铁律由两点承接：① 卡面文案全部中性轻松向（与星级解耦，无负面）；② 结果页星级基调副标 `content/starTone.ts`（1★=娱乐局）兜底展示。

**选卡真实化（首选锁定）**：抽卡页所选卡位（0–10）进入卡面种子——`cardForSlot(userId, date, reroll, slot, avoidCardId?)` 为独立确定性流，同 (u, date, reroll, slot) 恒同卡，卡位/日期不同为独立均匀抽取（连续多天同卡位不保证不同，但每天独立 1/10）；`lockCardPick()` 在选中时落库锁定（当天重进/分享卡展示同一张，星级等其余字段不变）。与最近一次（非当日）卡面去重：首抽相同则沿同一 rng 重抽一次（确定性，连续同卡 10% → 1%）。`cardFor()` 保留仅用于存量数据补齐（backfillMainCard）。

### 5.3 幸运位置（半随机，PRD §25）

权重构成：40% 随机 + 30% 常用位置（用户偏好）+ 20% 近期未推荐 + 10% 特殊事件。

**偏好档已接入（原 P1）**：`UserPrefs.role`（onboarding 常打位置）设置时走 40/30/20/10；未设置时维持 P0 降级（偏好并入随机档，70/20/10，行为逐字段不变）。偏好只作权重输入，不进种子、不改变 PRNG 抽取次数与顺序。

**P0 降级**（特殊事件仍为 P1，未设置时权重并入随机）：

- 随机 70% + 近期未推荐 20% + 特殊事件 10%（specialEventOf 为空实现，权重并入随机 → 实际 80%/20%）
- 近期未推荐：过去 7 天结果中**未出现**的角色平分该权重；若 7 天覆盖全部角色则退回随机
- 预留 `specialEventOf(date: string): Role | null` 钩子，P0 恒返回 null

四位置指数：primary = 5★；其余 3 个位置从 [4,3,2] 洗牌分配（保证不与主推荐并列）。

### 5.4 幸运英雄

- 从 primary 位置的英雄池中选；过去 7 天推荐过的英雄权重降为 1/3
- **偏好英雄 ×2（已接入）**：`UserPrefs.agents`（onboarding 常用英雄，归一化 id 匹配）在池内权重 2×（pref 6 : normal 3 : recent 1，recent 与偏好叠加为 2）；跨位置偏好不生效，池外 id（如 miks/veto）静默忽略
- 契合度 = 88 + floor(prng() × 9) → 88~96
- 关键词：从该英雄 keywords 池抽 3 个；blurb：从该英雄 blurbs 池抽 1 条

### 5.5 幸运武器

- 武器池抽 1 → 配 reason 模板；再从其余武器抽 1 作为 avoid → 配 avoid reason 模板
- avoid 文案要带梗（例：Operator →「今天的你可能会忍不住 Peek 第二次」）

### 5.6 幸运皮肤

- 皮肤池抽 1 → 契合度 = 90 + floor(prng() × 9) → 90~98
- 幸运颜色取该皮肤 colors 池随机 1；幸运饰品从 buddy 池抽 1；blurb 用模板

### 5.7 幸运地图

- 固定 5 图池，将 [5,4,3,2,1] 洗牌分配星级（互不重复）
- label 按星级从标签池取；降序排列后 [0] = 幸运地图，[4] = 今日雷区

### 5.8 今日 Advice

- advice 池抽 1；若与最近一次相同则重抽，最多尝试 3 次

### 5.9 改命（PRD §26）

- 条件：当日记录存在且 `reroll === 0`，且未被使用过（reroll 后记录本身即标记）
- 触发：生成 `reroll: 1` 的新结果（种子串追加 `|r1`），覆盖保存
- UI：结果页展示入口文案「命运并非不可改变 → 再抽一次」；reroll 后入口消失

### 5.10 每日刷新

- 所有页面读取的「今天」= `new Date()` 按本地时区格式化为 `YYYY-MM-DD`
- 跨 0 点后任何入口重新走生成逻辑（date 变 → 种子变 → 结果变），昨日结果自然进入 history

## 6. 内容库规格（core/content/）

> 文案总则（PRD §31.3）：每个字都要像玩家说的，不像星座博主说的。多用 VALORANT 术语与梗：Peek、干拉、烟、残局、红温、坐牢、1v5、手感、架点、转点。每库字段在开发时全量填充，本节约定了字段结构与规模。

### 6.1 heroes.ts — 英雄库（27 个，按位置分组）

```ts
export interface HeroEntry {
  id: string;          // 英文名（如 'Omen'），作为跨文件引用 key
  name: string;        // 展示名（英文名 + 常用中文昵称）
  role: Role;
  keywords: string[];  // 3–5 个风格关键词（如 冷静/信息差/偷 timing）
  blurbs: string[];    // 2–3 条趣味解释模板（含 {hero} 占位符）
}
```

| 位置 | 英雄 |
|---|---|
| duelist 决斗 | Jett, Raze, Reyna, Phoenix, Yoru, Neon, Iso, Waylay |
| initiator 先锋 | Sova, Breach, Skye, KAY/O, Fade, Gekko, Tejo |
| controller 控场 | Omen, Brimstone, Viper, Astra, Harbor, Clove |
| sentinel 哨卫 | Sage, Cypher, Killjoy, Chamber, Deadlock, Vyse |

blurb 示例（PRD 原句）：Omen →「今天别人看不到你的时候，往往就是你的机会。」

### 6.2 weapons.ts — 武器库（8 个）

```ts
export interface WeaponEntry {
  id: string;            // 'Phantom' | 'Vandal' | 'Operator' | ...
  name: string;          // 幻影 / 狂徒 / 大狙 ...
  reasons: string[];     // 2 条推荐理由模板
  avoidReasons: string[]; // 2 条「今日不推荐它」模板
}
```

池：Phantom 幻影、Vandal 狂徒、Operator 大狙、Sheriff、Spectre、Judge、Guardian、Bulldog。
理由示例：Phantom →「今天适合少一点赌枪，多一点稳定输出。」Operator(avoid) →「今天的你可能会忍不住 Peek 第二次。」

### 6.3 skins.ts — 皮肤库（≥20 个）

```ts
export interface SkinEntry {
  id: string;      // 'RGX Phantom' 等
  colors: string[];   // 幸运颜色候选（绿/紫黑/金白/霓虹粉/红金/蓝青…）
  blurbs: string[];   // 2 条契合文案模板
}
export const BUDDIES: string[];  // 饰品池：RGX Butterfly、战术小熊、骰子挂件、章鱼挂件、大魔王挂件…
```

候选（首版入库，允许扩充）：RGX 11z Pro、Reaver、Prime、Glitchpop、Kuronami、Oni、Araxys、Prelude to Chaos、Champions 2024、Protocol 781-A、Sentinels of Light、Neo Frontier、Ion、Origin、Sovereign、Singularity、Evori Dreamwings、Gaia's Vengeance、Cryostasis、Elderflame。

### 6.4 maps.ts — 地图池（5 个）

```ts
export interface MapEntry { id: string; name: string; }  // Ascent/Haven/Bind/Lotus/Breeze
export const MAP_LABELS: Record<Star, string[]>;  // 每星级 1–2 个标签
```

标签（PRD §18）：5★「上分圣地」/ 4★「可以一战」/ 3★「五五开」/ 2★「容易坐牢」/ 1★「今天最好别碰」。

### 6.5 advice.ts — 战术关键词库（≥16 个）

```ts
export interface AdviceEntry { keyword: string; note: string; }
```

首版入库（PRD §20 + 扩展）：

| keyword | note |
|---|---|
| 别急 | 今天你的枪法会奖励耐心。 |
| 相信第一判断 | 想太多反而会慢半拍。 |
| 少干拉 | 今天的大拉，可能只有背景板里才有你。 |
| 记得买枪 | 别省，省下来的钱不会陪你去下一局。 |
| 跟队友走 | 一个人冲上去的结局，通常是 1v5 教学局。 |
| 多看小地图 | 信息不会自己跑进你的眼睛。 |
| 不要压力枪 | 手越抖，枪越飘。 |
| 输两把就休息 | 连败是从「我还能打回来」开始的。 |
| 今天适合 Operator | 但也别五个人都起大狙。 |
| 今天别当英雄 | 该跑就跑，活着才有下一回合。 |
| DON'T REPEEK | 今天最大的敌人不是对面，而是你觉得自己还能再打一个。 |
| 稳住，别送 | 优势是苟出来的。 |
| 第三把输了就收手 | 事不过三。 |
| 不要相信你的 Operator | 今天的你可能会忍不住 Peek 第二次。 |
| 你今天的烟比枪靠谱 | 把舞台交给队友。 |
| 别急着 Peek | 等一个更好的时机。 |

### 6.6 cards.ts — 卡面内容库（主运核心，UI 融合新增）

```ts
export interface CardEntry {
  id: string;   // 'card-01'…'card-10'，与 src/assets/cards/*.webp 一一对应
  name: string; // 卡名，如「相信直觉」
  title: string;// 大字标题，如「别犹豫」
  read: string; // 解读段
  good: string; // 宜（「宜先动」格式）
  bad: string;  // 忌（「忌回头」格式）
}
export const CARD_POOL: CardEntry[]; // 10 条
```

10 张卡：01 注意协作 / 02 不动出击 / 03 团队优先 / 04 逆境破局 / 05 稳守阵线 / 06 慎用狙击 / 07 优先生存 / 08 把握时机 / 09 相信直觉 / 10 顺势而行（05/08/09 文案来自美术设计稿，逐字保留）。

文案铁律：游戏梗风格、禁星座腔、与星级解耦（任何星级读到都成立）、good/bad 带宜/忌前缀（2–4 字动宾）、read ≤ 60 字。

### 6.7 starTone.ts — 星级基调标签

```ts
export const STAR_TONE: Record<Star, string>; // 1:娱乐局 2:慢热 3:五五开 4:状态在线 5:火力全开
```

结果页主运副标使用，承接 1★「娱乐局」框架铁律（卡面文案与星级解耦后的兜底）。

## 7. 页面规格

> 流程状态机（App.tsx）：`home → pick → flip → result → share`，每步切换记录埋点。

### 7.1 Home（今日瓦运）

- 版式（设计稿 home 屏）：头部「每日一卦」标签 +「今日瓦运」+ 日期 `8月23日 星期日`（下边框）；三张 SVG 菱形符号卡背扇形（中间卡 106×188 发光）；大字标语「今天这把，有说法。」42px/900；主按钮「测测今日瓦运」54px 紫底；两个次级入口「今天用哪套皮肤？」「给队友算一卦」（P1 占位，同走 onStart 流程）
- 有历史：头部下显示昨日运势 mono 短横条（昨日关键词 + 星级）
- 已有今日结果：CTA 仍为「测测今日瓦运」，**每次进入都走完整仪式**（抽卡 → 卡面解读 → 运势，不跳转）；已有结果不重新生成，仪式展示同一份确定性结果（PRD §26 一致性不变）
- 页脚：「仅供娱乐 · 非官方产品」
- 验收：首屏一屏内；375px 不破版；重进结果不变（仪式可重复走）

### 7.2 CardPick（抽卡）

- 版式（设计稿 draw 屏）：顶部返回箭头 +「第二步 · 抽牌」；「凭感觉，选一张。」；三张卡背（菱形符号 SVG）
- 交互（手动流程）：点击任一卡 → 450ms 后放大居中 3D 翻转，正面显示**当日真实卡面图**（`cardImageUrl(main.cardId)`）；再点可翻回；其余卡下沉淡出；底部出现「你抽到了 {卡名}」+「卡面解读」按钮 → 进入 FlipReveal
- 说明：三张卡指向**同一份**当日运势（PRD §26 一致性），选择是仪式不是随机源
- 验收：点击任意卡均能到达结果；重复进入当日结果一致

### 7.3 FlipReveal（翻牌揭晓）

- 版式（设计稿 reveal 屏）：头部「已翻开 · {卡名}」+ 日期；卡面图 158×281 发光描边 + 翻转入场；五星行（紫色实心/描边空心）；大字标题 46px/900（卡面 title）+ 解读段（卡面 read）；**宜/忌双栏块**（数据源 `main.good` / `main.bad`）；按钮「查看详细运势」
- 动画：卡面 rotateY 翻转入场 0.7s + 页面 vRise 0.45s
- 无障碍：`prefers-reduced-motion` 时全部静默

### 7.4 FortuneResult（完整运势）

版式（设计稿 fortune 屏），内容顺序固定：

1. **主运副标行**：`主运 · {title}（{卡名}）` + 五星 + 星级基调标签（STAR_TONE）
2. **今日角色卡**：幽影立绘（27 英雄图补齐前固定幽影图）+ 职业名 + 英雄名 44px + 契合度 % + 关键词 chips ×3 + blurb；卡底部四位置指数 mono 四格（primary 高亮）+ reason + 同位置推荐英雄
3. **幸运武器卡**：武器名 + 理由 + 「今日别碰：{avoid}」（PRD §16 保留）
4. **幸运地图卡**：「今日有说法」角标 + 地图名 + 星级 + 标签
5. **幸运颜色 + 推荐皮肤**：色块 + 颜色名 + blurb；皮肤名 + 契合度；幸运饰品
6. **地图运势全列表**：5 图 mono 行（幸运地图紫标 / 雷区深紫标）+ 雷区行
7. **今日忌大块**（浅底反色）：大字 = `advice.keyword`，解释 = `advice.note`

- 底部：「生成我的今日瓦运」（进 ShareCard）「返回首页」
- 「改命」入口（文案「命运并非不可改变」）：仅当 reroll=0 时显示；点击 → 确认 → 重抽 → 刷新本页
- 「发给队友」复制移至分享页（§7.5）
- 验收：核心字段集中在前 1.5 屏（截图友好）；全部文案无星座腔；1★ 为娱乐局框架

### 7.5 ShareCard（分享卡）

- Canvas 绘制规格：逻辑尺寸 750×1334（9:16），渲染倍率 ×2（1500×2668）保证清晰度
- 版面（设计稿 2×2 网格版）：渐变底 `#1a1224→#100c17` + 22px 网格纹理 + 直角描边 → 头部品牌菱形 +「今日瓦运」（预留 `nickname` 选项，P1）+ 日期 → 五星行（手绘星形路径）→ 大字标题 `main.title` 92px → 2×2 网格：今日角色（职业 · 英雄）/ 幸运武器 / 幸运地图 / 今日忌（紫底反色 = `main.bad`）→ 底部品牌 + 二维码占位纹理 → 「仅供娱乐 · 非官方产品」页脚
- 页面配三个动作（平台适配层 `src/share/shareOut.ts`，逐级降级永远有兜底）：
  - 「保存图片」：iOS Safari → 弹系统分享面板（「存储图像」）；安卓/桌面 → `a[download]` 直接下载；微信内置浏览器 → toast「长按保存」
  - 「分享给朋友」：移动端 → 系统分享面板直接发分享卡图片文件（微信/QQ 收到图）；桌面 → 复制图片到剪贴板（聊天窗口粘贴）；再降级 → 分享「文案 + 链接」（部署后自动带页面地址）→ 复制文案 → 长按兜底
  - 「返回首页」：ghost 按钮
- 验收：真机（Android + iOS 微信）保存成功、分享面板弹出且微信/QQ 可发送；375px 布局不破

## 8. 设计规范

> 依据美术设计稿「Daily Valorant Fortune」（`design/unpacked/`，方向：电竞终端数据感 · 霓虹紫）。

- 基调：**电竞终端数据感**。底色紫黑 `#0c0910`（body `#0a0810`），面板 `#130f1a`，霓虹紫 accent `#c084d8`（oklch(0.72 0.19 300) 的 sRGB 近似），星级为紫色实心 + `acc-30` 描边空心；**全直角无圆角**
- 全局氛围：26px 网格线背景（`--acc-04`）+ 9s 循环扫描线（`--acc-08` 渐变条），挂在 App 顶层两个 fixed overlay（`.app-grid` / `.app-scanline`）
- `styles/tokens.css`：色板（`--acc` 及 04/08/16/30/55 透明度变体、`--ink` 及 60/45/30/14 变体、`--btn-ink`、`--on-light`、`--acc-deep`）、字号（10/11/12/13.5/14/16/22/36/42/46）、间距、glow 阴影全套 token；页面只引用 token 不写死值
- 移动端：`viewport` meta、375px 设计基准、`env(safe-area-inset-bottom)` 防微信底部遮挡
- 字体：Noto Sans SC（400/500/700/900）+ JetBrains Mono（400/500/700/800，数据/标签/步骤编号），Google Fonts `display=swap` 加载 + 系统 fallback 栈（PingFang SC / 微软雅黑 / Cascadia Code / Consolas）兜底国内访问
- **素材**：10 张卡面 + 幽影立绘/头像 WebP 化进 `src/assets/`（`scripts/compress-assets.mjs` 一次性管线，总量 ≤2MB），`src/ui/cardAssets.ts` 负责 id→URL 映射；core 层只存 cardId 字符串（图标 emoji 与「零图片资源」旧条款废除）
- 星级组件：`Stars`（★ 实心/空心 + 逐颗渐显）与手绘 SVG 星形（Canvas 分享卡）两套，全局复用

## 9. 埋点（analytics.ts，北极星指标来源 PRD §33）

`track(name: string, props?: Record<string, unknown>)` 抽象，**已接入百度统计**：部署后注册站点（tongji.baidu.com）→ 把统计 ID 填入 `src/analytics.ts` 的 `BAIDU_TONGJI_ID` → 重新构建即可；ID 为空时仅开发态 console。事件走 `_hmt.push(['_trackEvent', 'gg_oracle', name, label])`，换其它统计服务只改此文件。事件不携带个人内容（昵称/运势详情均不上报）。

| 事件 | 触发点 |
|---|---|
| `view_home` | 进入首页 |
| `view_reveal` | 已有今日结果时直达揭晓页 |
| `onboard_finish` / `onboard_skip` | 完成/跳过 onboarding |
| `settings_save` | 「我的设定」保存偏好 |
| `test_start` | 点击抽卡 CTA / 进入 CardPick |
| `card_pick` | 选卡落锁（首选锁定） |
| `fortune_complete` | 结果页渲染完成 |
| `share_open` | 打开分享卡 |
| `share_save` | 点击保存图片（含 iOS 系统面板路径） |
| `share_copy` | 点击分享给朋友（含系统分享面板/桌面复制图片路径） |

北极星计算：**完成率** = `fortune_complete` / `test_start`（目标 ≥80%）；**分享率** = (`share_save` + `share_copy`) / `fortune_complete`（目标 ≥15%）。

## 10. 开发里程碑（按顺序执行，每步验收通过才进下一步）

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M1 脚手架与内容库** | Vite+React+TS 初始化；目录结构；tokens.css；core/types、random、全部 content 库（§6 全量填充） | `npm run dev` 可跑；PRNG 单测通过 |
| **M2 核心算法** | generator.ts 全模块生成；storage.ts 持久化；改命逻辑 | 同参数两次生成逐字段 deep-equal；跨天结果变化；改命仅一次；§11 全部算法单测通过 |
| **M3 页面流程** | Home → CardPick → FlipReveal → FortuneResult 五页 + 动画 + 埋点 | 全流程 ≤30s/≤4 次点击；重进结果一致；首次欢迎态正确；375px 不破版 |
| **M4 分享卡** | drawShareCard.ts Canvas 绘制 + 保存/复制 | 真机（Android + iOS 微信）保存成功 |
| **M5 打磨与部署** | 文案全量检查（无星座腔/无负面）；reduced-motion；埋点接入；静态部署 HTTPS | §3 全部验收标准通过；线上可用 |

## 11. 测试清单（Vitest，只测 core）

1. **确定性**：`genDailyFortune` 同参数两次调用 deep-equal
2. **种子隔离**：不同 userId / 不同日期 / reroll 0 与 1 之间结果不同（抽样断言）
3. **改命**：reroll=1 生成后，再次请求被拒；结果覆盖保存
4. **位置权重**：无偏好时用固定 userId 序列模拟 1000 天，随机/未推荐比例近似 80/20；连续 7 天同一位置出现率为 0；偏好设置时 400 天对照偏好位置出现率显著高于无偏好（统计测试全种子化）
5. **星级分布**：10 万次抽样与 §5.2 表误差 <2%
6. **地图**：5 图星级互不重复且恰好为 1–5 的排列
7. **历史**：90 天上限生效；昨日运势查找正确（含跨月）

## 12. 风险与合规

- **商标**：英雄/皮肤名属 Riot Games，仅作描述性提及；页脚常驻「仅供娱乐 · 非官方产品」
- **素材版权**：10 张卡面为用户自绘；幽影立绘/头像为官方角色素材（临时占位），上线前需确认素材来源与使用合规
- **无真实预测声明**：PRD §19 强调娱乐属性，任何文案不得暗示真实预测能力
- **不接游戏数据 API**：PRD §17 明确首版不做库存读取（也无合法 API），皮肤为全池随机
- **不涉付费抽奖**：PRD §24 明确卡牌无真实价值，不做任何付费点
- **微信内分享限制**：H5 链接分享无法自定义卡片（需公众号 JS-SDK），P0 以「保存图片 + 复制文案」为主路径
- **iOS 下载兼容**：`a[download]` 在 iOS Safari/微信内不可靠 → 长按保存提示兜底
- **隐私**：全部数据仅存本地，不上传（页脚注明）

## 13. 后续演进预留（不在 P0 实现，仅保证可扩展）

| 项 | 来源 | 预留方式 |
|---|---|---|
| 玩家偏好设置 | §10, P1 | **全部完成**：算法接入（§5.3/§5.4，onboarding 数据 → `UserPrefs` 权重）+ 编辑入口（首页「我的设定」→ Onboard edit 模式；改偏好次日生效，今日已抽运势不变） |
| 稀有卡 70/20/8/2 | §24, P1 | 卡面池已就位（10 张）；加稀有度抽取，输出映射到卡片视觉层级 |
| 皮肤库模式 B（只测我拥有的） | §17, P1 | skins 池加 `owned` 过滤入口；首页次级入口「今天用哪套皮肤？」已占位 |
| 签到/历史/图鉴/分享记录 | §29, P1 | 存储结构已含全部 fortunes，图鉴从历史聚合 |
| 好友双排/五排阵容 | §22–23, P1 | core 加 `genDuo(a,b,date)` / `genSquad(names,date)` 纯函数；首页「给队友算一卦」已占位 |
| 今日游戏方式（推荐模式/时段） | §19 | DailyFortune 加可选 `playstyle` 字段 |
| 底部四 Tab（今日/朋友/图鉴/我的） | §29 | P0 单流程状态机不影响；P1 引入 tab 容器 |
| 多游戏（LOL/王者/CS2/Apex/OW） | P2 | core/content 按游戏目录化，generator 以 `GameConfig`（维度定义）参数化 |
| **迁移微信小程序** | 既定路线 | `core/` 原样复用；`src/` 换 `pages/`；storage 换 `wx.setStorageSync`；分享卡换离屏 canvas + `onShareAppMessage`；埋点换微信统计 |

### 13.1 UI 融合（设计稿）变更点备忘

- tokens 色板整体替换为设计稿（紫黑 + 霓虹紫 + 全直角）；旧 token 别名在页面重绘完成后已移除
- `MainLuck` 增加 `cardId/cardName/good/bad`；`mainLuck.ts` 删除，由 `cards.ts` + `starTone.ts` 替代
- 卡面为独立种子流 `cardFor()`，不消耗主 RNG；存量数据由 `backfillMainCard()` 在 App 初始化处补齐（无感迁移，90 天历史保留）
- 素材管线：`scripts/compress-assets.mjs`（npx sharp 一次性脚本）→ `src/assets/`（WebP ≤2MB）
- 卡面 PNG 与幽影立绘为第三方素材：幽影图为官方角色立绘（27 英雄图补齐前临时固定使用），上线前需团队确认素材来源合规

## 14. 部署说明（H5 静态托管）

产物在 `dist/`（`npm run build` 生成，约 74KB gzip），纯静态、无后端，任意静态托管均可：

| 平台 | 方式 | 备注 |
|---|---|---|
| Vercel | 关联仓库自动部署，Build: `npm run build`，Output: `dist` | 推荐，自带 HTTPS 与国内可访问的 CDN 加速 |
| Netlify | 同上，Publish directory: `dist` | 与 Vercel 等效 |
| GitHub Pages | 推送 `dist`（或 Actions 自动部署） | `base: './'` 已配置，子路径可用 |
| Cloudflare Pages | 关联仓库，Build: `npm run build`，Output: `dist` | 国内速度好 |

要求与注意：

- **必须 HTTPS**：微信内置浏览器对 HTTP 页面有安全限制
- 推送前初始化 git 仓库并关联远端（部署平台均以仓库为源）
- 部署后真机验收清单：微信内打开 → 全流程 ≤30s → 重进结果一致 → 分享卡长按可保存 → 改命仅一次
- 埋点上线前接入统计服务：改 `src/analytics.ts` 一处即可（当前为 console 输出）
